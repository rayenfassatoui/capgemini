\begin{tikzpicture}[node distance=0.85cm and 1.0cm, font=\small]
  \node[actor] (user) {Utilisateur};
  \node[box, right=2.2cm of user] (route) {Route SSE\\\sourcecode{/api/chat/statistics}};
  \node[box, right=of route] (context) {Contexte\\session + rôle\\historique nettoyé};
  \node[box, below=of context] (tools) {Filtrage outils\\RBAC + Zod};
  \node[box, below=of route] (llm) {Boucle agentique\\max 8 étapes};
  \node[box, right=of llm] (confirm) {Confirmation\\actions sensibles};
  \node[box, right=of confirm] (exec) {Exécution outil\\RBAC + Zod};
  \node[database, below=of exec] (db) {Base\\données};
  \node[box, below=of llm] (ground) {Sanitization\\grounding};
  \node[box, left=of ground] (cards) {Métadonnées\\sources + graphiques\\cartes typées};
  \node[box, below=of cards] (trace) {Trace par phases\\retrieval, analysis\\confirmation, execution};
  \node[box, left=of trace] (ui) {Réponse UI\\source-backed\\actions de suivi};

  \draw[line] (user) -- (route);
  \draw[line] (route) -- (context);
  \draw[line] (context) -- (tools);
  \draw[line] (tools) -- (llm);
  \draw[line] (llm) -- (confirm);
  \draw[line] (confirm) -- (exec);
  \draw[line] (exec) -- (db);
  \draw[line] (db.west) -| (llm.south);
  \draw[line] (llm) -- (ground);
  \draw[line] (ground) -- (cards);
  \draw[line] (cards) -- (trace);
  \draw[line] (trace) -- (ui);
  \draw[line] (ui.west) -| (user.south);
\end{tikzpicture}
