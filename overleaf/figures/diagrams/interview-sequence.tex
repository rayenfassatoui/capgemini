\begin{tikzpicture}[node distance=0.9cm and 1cm, font=\small]
  \node[actor] (actor) {TA / Manager / HR};
  \node[box, right=of actor] (schedule) {Planifier entretien\\date, heure, meet link};
  \node[box, right=of schedule] (notif) {Créer notification\\et log d'activité};
  \node[box, below=of schedule] (mail) {Envoyer invitation\\email};
  \node[box, right=of mail] (calendar) {Agenda\\journalier / plage};
  \node[box, below=of mail] (report) {Saisir rapport\\score + décision};
  \node[box, left=of report] (stage) {Mettre à jour\\le stage candidat};

  \draw[line] (actor) -- (schedule);
  \draw[line] (schedule) -- (notif);
  \draw[line] (schedule) -- (mail);
  \draw[line] (mail) -- (calendar);
  \draw[line] (calendar) -- (report);
  \draw[line] (report) -- (stage);
\end{tikzpicture}
