\begin{tikzpicture}[node distance=0.9cm and 1cm, font=\small]
  \node[actor] (ta) {TA};
  \node[box, right=of ta] (upload) {Upload document\\PDF / DOCX};
  \node[box, right=of upload] (parse) {Parsing\\texte brut};
  \node[box, right=of parse] (extract) {Extraction IA\\nom, email, skills...};
  \node[box, below=of parse] (store) {Mise à jour\\\sourcecode{cv\_pool}};
  \node[box, right=of store] (embed) {Embedding\\1024 dims};
  \node[box, right=of embed] (chunk) {Chunking + FTS\\\sourcecode{cv\_chunks}};

  \draw[line] (ta) -- (upload);
  \draw[line] (upload) -- (parse);
  \draw[line] (parse) -- (extract);
  \draw[line] (extract) -- (store);
  \draw[line] (store) -- (embed);
  \draw[line] (embed) -- (chunk);
  \draw[dashedline] (parse) -- (store);
\end{tikzpicture}
