\begin{tikzpicture}[
  font=\scriptsize,
  umlclass/.style={
    draw=cgdeep,
    rounded corners=2pt,
    thick,
    fill=white,
    align=left,
    inner sep=4pt,
    text width=2.85cm
  },
  association/.style={thick, draw=cgdeep},
  composition/.style={{Diamond[open,length=2.3mm,width=2.3mm]}-, thick, draw=cgdeep},
  relationlabel/.style={font=\tiny, fill=white, inner sep=1pt},
  multiplicity/.style={font=\tiny, fill=white, inner sep=1pt}
]
  \node[umlclass] (user) at (0,0) {
    \centering\textbf{User}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{email}: text\\
    \texttt{role}: enum
  };

  \node[umlclass] (job) at (3.55,0) {
    \centering\textbf{Job}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{title}: text\\
    \texttt{seniority}: text
  };

  \node[umlclass] (candidate) at (7.1,0) {
    \centering\textbf{Candidate}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{stage}: enum\\
    \texttt{taId}: uuid\\
    \texttt{managerId}: uuid
  };

  \node[umlclass] (interview) at (10.65,0) {
    \centering\textbf{Interview}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{scheduledAt}: date\\
    \texttt{status}: enum
  };

  \node[umlclass] (cvpool) at (0,-3.05) {
    \centering\textbf{CvPool}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{rawText}: text\\
    \texttt{skills}: text[]\\
    \texttt{embedding}: vector
  };

  \node[umlclass] (screening) at (7.1,-3.05) {
    \centering\textbf{Screening}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{score}: integer\\
    \texttt{summary}: text\\
    \texttt{gaps}: json
  };

  \node[umlclass] (guide) at (10.65,-3.05) {
    \centering\textbf{InterviewGuide}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{questions}: json\\
    \texttt{autopilot}: boolean
  };

  \node[umlclass] (chunk) at (0,-6.1) {
    \centering\textbf{CvChunk}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{chunkText}: text\\
    \texttt{searchVector}: tsvector\\
    \texttt{embedding}: vector
  };

  \node[umlclass] (governance) at (7.1,-6.1) {
    \centering\textbf{GovernanceAudit}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{kind}: enum\\
    \texttt{status}: enum
  };

  \node[umlclass] (report) at (10.65,-6.1) {
    \centering\textbf{InterviewReport}\par
    \noindent\rule{\linewidth}{0.35pt}\par
    \texttt{id}: uuid\\
    \texttt{score}: integer\\
    \texttt{decision}: enum
  };

  \draw[association] (user.east) -- node[relationlabel, above]{crée}
    node[multiplicity, above, pos=0.16]{1} node[multiplicity, above, pos=0.84]{0..*} (job.west);
  \draw[association] (job.east) -- node[relationlabel, above]{cible}
    node[multiplicity, above, pos=0.16]{1} node[multiplicity, above, pos=0.84]{0..*} (candidate.west);
  \draw[association] (candidate.east) -- node[relationlabel, above]{planifie}
    node[multiplicity, above, pos=0.16]{1} node[multiplicity, above, pos=0.84]{0..*} (interview.west);

  \draw[association] (user.south) -- node[relationlabel, left]{importe}
    node[multiplicity, left, pos=0.18]{1} node[multiplicity, left, pos=0.82]{0..*} (cvpool.north);
  \draw[association] (cvpool.east) -- node[relationlabel, above, sloped]{alimente}
    node[multiplicity, below, sloped, pos=0.16]{1} node[multiplicity, below, sloped, pos=0.86]{0..*} (candidate.south west);
  \draw[composition] (cvpool.south) -- node[relationlabel, left]{découpe}
    node[multiplicity, left, pos=0.18]{1} node[multiplicity, left, pos=0.82]{1..*} (chunk.north);

  \draw[association] (candidate.south) -- node[relationlabel, left]{évalue}
    node[multiplicity, left, pos=0.18]{1} node[multiplicity, left, pos=0.82]{0..1} (screening.north);
  \draw[association] (interview.south) -- node[relationlabel, right]{prépare}
    node[multiplicity, right, pos=0.18]{1} node[multiplicity, right, pos=0.82]{0..1} (guide.north);
  \draw[association] (guide.south) -- node[relationlabel, right]{produit}
    node[multiplicity, right, pos=0.18]{1} node[multiplicity, right, pos=0.82]{0..1} (report.north);

  \draw[association] (screening.south) -- node[relationlabel, left]{journalise}
    node[multiplicity, left, pos=0.18]{1} node[multiplicity, left, pos=0.82]{0..*} (governance.north);
  \draw[association] (report.west) -- node[relationlabel, above]{trace}
    node[multiplicity, above, pos=0.16]{1} node[multiplicity, above, pos=0.84]{0..*} (governance.east);
\end{tikzpicture}
